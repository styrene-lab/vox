[extension]
name = "vox"
version = "0.1.0"
description = "Slack connector for omegon"

[runtime]
type = "native"
binary = "bin/vox"

[startup]
ping_method = "get_tools"
timeout_ms = 10000

[secrets]
required = []
optional = ["VOX_SLACK_BOT_TOKEN", "VOX_SLACK_APP_TOKEN"]
