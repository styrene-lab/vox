[extension]
name = "vox"
version = "0.1.0"
description = "Discord connector for omegon"

[runtime]
type = "native"
binary = "bin/vox"

[startup]
ping_method = "get_tools"
timeout_ms = 10000

[secrets]
required = []
optional = ["VOX_DISCORD_BOT_TOKEN"]
